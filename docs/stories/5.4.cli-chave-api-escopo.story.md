# Story 5.4: CLI autenticada por chave de API com escopo

**Status:** Done
**Prioridade:** High
**Dependências:** Story 5.3
**Epic:** 5

depends_on: 5.3

## Executor Assignment

executor: "@dev"
quality_gate: "@architect"
quality_gate_tools: ["security-tests", "contract-tests", "cli-smoke", "telemetry"]

## Story

**Como** usuário da CLI,
**quero** autenticar a integração por chave revogável e com escopo,
**para que** a CLI não dependa de uma sessão web legada nem armazene senha de usuário.

## Acceptance Criteria

1. A CLI deixa de chamar o endpoint removido `/api/auth/login` e usa o contrato de chave MCP/API existente ou um fluxo explicitamente versionado.
2. A chave possui escopos mínimos para as operações da CLI, validade/revogação verificáveis e armazenamento local protegido contra exibição acidental.
3. Erros distinguem chave ausente, inválida, expirada/revogada e sem escopo, sem revelar segredos.
4. O uso do fallback de sessão legado em `apps/api/src/lib/session.ts` é medido com logs/métricas sanitizados por tipo de consumidor.
5. A compatibilidade legada da CLI pode ser desativada por configuração após a janela de observação, com rollback documentado.
6. Testes de contrato e E2E cobrem emissão/provisionamento, uso permitido, negação, revogação e nova autenticação.
7. Documentação e ajuda da CLI deixam claro como configurar e rotacionar a chave.

## 🤖 CodeRabbit Integration

> **CodeRabbit Integration**: Disabled
>
> CodeRabbit CLI is not enabled in `core-config.yaml`. Quality validation will use manual review process only.

## Tasks / Subtasks

- [x] Inventariar comandos e escopos mínimos da CLI (AC: 1, 2).
- [x] Implementar configuração/autenticação por chave sem senha (AC: 1-3).
- [x] Adicionar telemetria sanitizada ao fallback legado (AC: 4).
- [x] Adicionar flag, janela de observação e rollback (AC: 5).
- [x] Cobrir contratos e smoke CLI ponta a ponta (AC: 6).
- [x] Atualizar ajuda e documentação de rotação/revogação (AC: 7).

## Dev Notes

- `packages/cli/src/index.mjs` ainda chama `/api/auth/login`; esse contrato não existe na aplicação Next.js atual.
- `apps/api/src/lib/session.ts` já reconhece chave MCP com escopo antes do fallback legado.
- Não colocar chave em argumento de processo, URL, log, snapshot ou mensagem de erro.
- A remoção final do fallback compartilhado só ocorre após Story 5.5 e evidência de uso zero; esta story retira a dependência da CLI.

### Testing

- testes de contrato para escopos e revogação
- CLI smoke contra Docker Compose
- inspeção de logs para ausência de segredo
- regressão E2E de chave MCP da Story 5.3

## Change Log

| Date | Version | Description | Author |
|---|---|---|---|
| 2026-08-20 | 0.1.0 | Draft criado a partir do Epic 5 | @sm |
| 2026-08-20 | 0.1.1 | Validated GO (9/10) — Status: Draft → Ready | @po |

## PO Validation

**Verdict:** GO — 9/10, confiança alta. Escopos, telemetria, revogação, rollout e proteção de segredo estão explícitos.

## Dev Agent Record

### Agent Model Used

Codex (GPT-5)

### Debug Log References

- `npm run lint`
- `npm run typecheck`
- `npm test`
- `npm run build:api`
- `npm run e2e:critical` — 15/15, desktop e mobile
- `npm run validate:structure`
- `npm run validate:agents`
- `npm run sync:ide:check`

### Completion Notes List

- Removido o login por email/senha da CLI; a chave entra somente por variável de ambiente ou `stdin` e é validada para `read` + `sync` antes de ser persistida.
- Erros canônicos distinguem ausência, invalidez, expiração, revogação observada e escopo insuficiente sem retornar a chave.
- O Perfil separa emissão de chave CLI mínima e token MCP com `mapping`; rotação/revogação foram cobertas no Docker E2E.
- O fallback legado tem kill switches global e específico da CLI e telemetria sanitizada por consumidor; a remoção definitiva continua dependente da Story 5.5.
- Durante o primeiro E2E, a CLI respeitou incorretamente a URL persistida em vez de `PARIDADE_API_URL`; a precedência env-first foi corrigida e ganhou regressão automatizada.

### File List

- `apps/api/docker-compose.hetzner.yml`
- `apps/api/src/app/api/auth/mcp-token/validate/route.ts`
- `apps/api/src/app/perfil/page.tsx`
- `apps/api/src/lib/session-policy.test.ts`
- `apps/api/src/lib/session.ts`
- `docker-compose.e2e.yml`
- `docker-compose.yml`
- `docs/cli-api-key-auth.md`
- `docs/multi-interface-smoke-tests.md`
- `docs/stories/5.4.cli-chave-api-escopo.story.md`
- `package-lock.json`
- `packages/cli/package.json`
- `packages/cli/src/index.mjs`
- `packages/cli/test/auth.test.mjs`
- `packages/shared/src/http-client.mjs`
- `packages/shared/test/http-client.test.mjs`
- `tests/e2e/critical-flows.spec.ts`

## QA Results

### Architecture Gate — PASS

Revisão de `@architect` aprovada sem issues imediatas. Os sete critérios possuem rastreabilidade em testes unitários/contratuais e no Docker E2E. Evidências: `docs/qa/story-5.4-cli-api-key-evidence-2026-08-20.md`; gate: `docs/qa/gates/5.4-cli-chave-api-escopo.yml`.
