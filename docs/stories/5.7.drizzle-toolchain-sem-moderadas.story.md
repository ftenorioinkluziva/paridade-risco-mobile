# Story 5.7: Remediar vulnerabilidades moderadas do toolchain Drizzle

**Status:** Done
**Prioridade:** High
**Dependências:** Story 5.2
**Epic:** 5

depends_on: 5.2

## Executor Assignment

executor: "@data-engineer"
quality_gate: "@dev"
quality_gate_tools: ["npm-audit", "drizzle-kit", "postgres", "migration-dry-run"]

## Story

**Como** mantenedor da plataforma,
**quero** atualizar o toolchain Drizzle por um caminho suportado,
**para que** as quatro vulnerabilidades moderadas restantes sejam eliminadas sem corromper schema ou histórico de migrações.

## Acceptance Criteria

1. O audit inicial registra a cadeia exata das quatro moderadas ligada a `drizzle-kit`/esbuild e separa dependências de desenvolvimento das de produção.
2. É escolhida uma combinação suportada de `drizzle-kit`, `drizzle-orm` e peer dependencies; downgrade forçado para versão incompatível não é aceito.
3. O schema e todas as migrações versionadas aplicam do zero em PostgreSQL descartável.
4. Upgrade sobre uma cópia da baseline existente é validado com backup e rollback documentados.
5. `db:generate` não produz drift inesperado; qualquer mudança de formato/metadados é revisada e versionada conscientemente.
6. `npm audit` deixa de reportar as quatro moderadas do toolchain Drizzle e não introduz high/critical.
7. Build, typecheck, testes unitários/integração e smoke Docker passam com o toolchain atualizado.
8. A decisão de versão, impactos e procedimento de manutenção ficam documentados.

## 🤖 CodeRabbit Integration

> **CodeRabbit Integration**: Disabled
>
> CodeRabbit CLI is not enabled in `core-config.yaml`. Quality validation will use manual review process only.

## Tasks / Subtasks

- [x] Capturar audit e grafo de dependências inicial (AC: 1).
- [x] Verificar caminho oficial de upgrade e compatibilidades (AC: 2, 8).
- [x] Atualizar dependências/lockfile em branch dedicada (AC: 2).
- [x] Validar migração limpa e upgrade de baseline descartável (AC: 3, 4).
- [x] Executar generate/diff e revisar metadados (AC: 5).
- [x] Executar audit e gates completos (AC: 6, 7).
- [x] Documentar decisão, backup e rollback (AC: 4, 8).

## Dev Notes

- Baseline observado: `drizzle-kit ^0.31.10` e `drizzle-orm ^0.45.2` em `apps/api/package.json`.
- A correção automática sugerida pelo audit pode implicar downgrade; não usar `npm audit fix --force` sem prova de compatibilidade.
- Migrações ficam em `apps/api/drizzle`; execução da aplicação usa `apps/api/src/db/migrate.ts`.
- A documentação oficial de upgrade v1 alerta para mudanças no formato de migrações; validar o procedimento contra a versão escolhida.

### Testing

- `npm audit --json` e `npm audit --omit=dev --json`
- `npm run db:generate --workspace @paridade-risco/api`
- migração do zero e upgrade em bancos PostgreSQL descartáveis
- `npm run lint`, `npm run typecheck`, `npm test`, `npm run build:api`
- smoke Docker com consulta de schema/health

## Change Log

| Date | Version | Description | Author |
|---|---|---|---|
| 2026-08-20 | 0.1.0 | Draft criado a partir do Epic 5 | @sm |
| 2026-08-20 | 0.1.1 | Validated GO (9/10) — Status: Draft → Ready | @po |
| 2026-08-20 | 1.0.0 | Toolchain remediado, metadados reconciliados e gates concluídos | @data-engineer |

## PO Validation

**Verdict:** GO — 9/10, confiança alta. O caminho evita downgrade forçado e exige prova sobre banco limpo e cópia da baseline.

## Dev Agent Record

### Agent Model Used

GPT-5 Codex

### Debug Log References

- `docs/qa/story-5.7-drizzle-toolchain-evidence-2026-08-20.md`
- PostgreSQL 16 descartável: fresh migrate, baseline/no-op, snapshot e restore.
- E2E smoke isolado: `smoke-mt1xsgdu-f1305c`.

### Completion Notes List

- Mantida a combinação estável `drizzle-kit@0.31.10` + `drizzle-orm@0.45.2`; rejeitados o downgrade automático e o v1 RC com mudança de formato/risco de drift conhecido.
- `drizzle-kit` foi centralizado na raiz para que o override de `esbuild@0.25.12` seja aplicado de modo reproduzível em workspaces por `npm ci`.
- `npm audit` e `npm audit --omit=dev` passaram com zero vulnerabilidades.
- O snapshot `0019_snapshot.json` reconcilia o metadata gap das migrations manuais 0016–0019; `db:generate` passou sem mudanças.
- PostgreSQL descartável confirmou 20 migrations, 24 tabelas, schema idêntico após upgrade/no-op e restore íntegro do backup.
- Lint, typecheck, 99 testes de workspace, 11 testes de integração, build e smoke Docker desktop/mobile passaram.

### File List

- `apps/api/drizzle/meta/0019_snapshot.json`
- `apps/api/package.json`
- `docs/qa/story-5.7-drizzle-toolchain-evidence-2026-08-20.md`
- `docs/stories/5.7.drizzle-toolchain-sem-moderadas.story.md`
- `package-lock.json`
- `package.json`

## QA Results

### Quality Gate — @dev — 2026-08-20

**Verdict:** PASS

- AC1–AC2: baseline, cadeia transitiva, classificação e decisão de versão estão documentadas; a combinação estável preserva o peer do Better Auth e rejeita downgrade/RC arriscados.
- AC3–AC5: PostgreSQL 16 descartável aplicou 20 migrations/24 tabelas do zero; migrate repetido manteve SHA-256 normalizado do schema e `db:generate` ficou sem mudanças após a reconciliação consciente do snapshot 0019.
- AC6: `npm audit` e `npm audit --omit=dev` reportam zero vulnerabilidades.
- AC7: lint, typecheck, 99 testes de workspace, 11 testes de integração, build e smoke Docker Chromium desktop/mobile passaram.
- AC8: decisão, riscos, backup, restore e manutenção estão registrados na evidência da story.
- Revisão do diff, lockfile, árvore npm e JSON do snapshot: sem findings bloqueantes ou regressões identificadas.

**Risco residual:** o override transitivo deve ser removido quando uma release estável do Drizzle eliminar `@esbuild-kit/esm-loader`; qualquer adoção do v1 deve seguir uma story própria para conversão do histórico. As 121 advertências sem erro de `validate:agents` pertencem à baseline do framework e não foram alteradas nesta story.
