---
id: '2.6'
title: Documentação e smoke tests multi-interface
status: Done
type: Quality
priority: High
executor: '@dev'
quality_gate: '@architect'
quality_gate_tools: [documentation_audit, end_to_end_smoke, security_regression]
---

# Story 2.6: Documentação e smoke tests multi-interface

## User Story

Como operador e agente consumidor, quero documentação fiel ao runtime e smokes reproduzíveis, para executar apenas comandos e tools realmente disponíveis.

## Status

Done

## 🤖 CodeRabbit Integration

> **CodeRabbit Integration**: Disabled
>
> CodeRabbit CLI is not enabled in `core-config.yaml`. Quality validation will use manual review process only.

## Scope

- Sincronizar Skill, README, exemplos de CLI/MCP e documentação de autenticação/idempotência.
- Remover ou marcar como planejadas as operações inexistentes auditadas.
- Criar smoke tests reproduzíveis para API, CLI, MCP local e MCP remoto.
- Registrar migração, rollback e troubleshooting.

## Out of Scope

- Implementar `update_prices_all` ou qualquer nova operação apenas para satisfazer documentação antiga.

## Tasks / Subtasks

- [x] Inventariar documentação contra os catálogos executáveis (AC: 1, 3).
- [x] Corrigir referências a comandos/tools inexistentes e contratos legados (AC: 1, 2, 4).
- [x] Criar smokes de descoberta, sucesso, input inválido, auth e erro externo (AC: 3, 4).
- [x] Verificar idempotência e ausência de segredo em logs no fluxo integrado (AC: 2–5).
- [x] Executar todos os gates, registrar resultados e atualizar a File List.

## Acceptance Criteria

1. Toda operação documentada existe e todo exemplo usa o contrato atual.
2. A documentação descreve Bearer auth, erros canônicos, `retryable` e idempotência.
3. Smokes cobrem as quatro interfaces e falham diante de divergência contratual.
4. Não há token em URL, exemplo, schema ou saída de log coletada.
5. Gates do repositório e smoke integrado têm resultados registrados nesta story.

## Dependencies

- Depende das Stories 2.2, 2.3, 2.4 e 2.5.

## Testing

- Smoke de API, CLI, MCP local e MCP remoto.
- `npm run lint`, `npm run typecheck`, `npm test`, `npm run build`.

## Dev Notes

- A Skill atual anuncia `update_prices_all`, `prices update all` e `prices update one`, mas os adaptadores auditados não registram essas operações. Corrigir a documentação; não implementar novas operações nesta story.
- Fontes a confrontar: `skills/paridade-risco/SKILL.md`, READMEs, `packages/cli/src/index.mjs`, `packages/local-mcp/src/index.mjs`, `apps/remote-mcp/src/index.mjs` e rotas públicas da API.
- Os smokes devem ser reproduzíveis e cobrir API, CLI, MCP local e remoto, incluindo regressões de segurança e idempotência entregues no épico.

## Checklist

- [x] Scope validado
- [x] Tasks concluídas
- [x] AC atendidos
- [x] Gates executados
- [x] File List atualizada

## File List

- [x] `skills/paridade-risco/SKILL.md`
- [x] `.mcp.example.json`
- [x] `docs/remote-mcp-auth-migration.md`
- [x] `docs/multi-interface-smoke-tests.md`
- [x] `docs/stories/2.4.paridade-cli-mcp.story.md`
- [x] `docs/stories/2.6.documentacao-smoke-tests.story.md`

## Dev Agent Record

### Agent Model Used

GPT-5

### Debug Log References

- `rg "update_prices_all|prices update|PARIDADE_USER_ID|/:token/mcp|http://[^\s]*<token>|/\{?token\}?/mcp" ...` — apenas mencoes negativas/historicas em docs de story; docs operacionais sem contrato legado publicado.
- `npm run lint` — passou com 7 warnings preexistentes, sem erros.
- `npm run typecheck` — passou.
- `npm test` — passou, 36 testes.
- `node --test packages/shared/test/adapter-parity.test.mjs` — passou, 3/3 smokes de paridade.
- `npm run test:integration --workspace @paridade-risco/api` — passou, 11/11 testes de idempotencia contra PostgreSQL real.
- `npm run build:api` — compilou e retornou exit code 0; Next.js manteve warning conhecido de autopatch de lockfile/SWC em workspace.

### Completion Notes List

- Skill `paridade-risco` sincronizada com o catalogo canonico: removeu comandos/tools inexistentes de update de precos e documentou as nove operacoes reais.
- Exemplo MCP removeu `PARIDADE_USER_ID`; identidade agora e derivada da sessao Bearer.
- Documentacao operacional cobre Bearer auth, erro canonico, `retryable`, idempotencia, smoke tests e troubleshooting/rollback sem publicar token no path como exemplo.
- Smoke reproduzivel aponta para suites executaveis de shared, remote MCP, API e integracao de idempotencia.

## QA Results

### Review Date: 2026-07-10

### Reviewed By: Quinn (Test Architect)

### Code Quality Assessment

A documentacao operacional esta alinhada ao runtime: catalogo de operacoes vem de `packages/shared/src/contracts.mjs`, a skill nao anuncia comandos inexistentes, o exemplo MCP nao transporta usuario por header/env legado e os smokes executaveis cobrem paridade, auth, validacao externa e idempotencia.

### Compliance Check

- Documentation Audit: ✓ docs operacionais sincronizadas com CLI/MCP/API.
- End-to-End Smoke: ✓ suites reproduziveis registradas e executadas.
- Security Regression: ✓ sem token em URL em exemplo operacional; logs redigidos cobertos por teste.
- All ACs Met: ✓ todos os ACs atendidos.

### Requirements Traceability

- AC1: PASS — toda operacao documentada existe no catalogo canonico; operacoes antigas de update de precos foram marcadas como indisponiveis.
- AC2: PASS — docs descrevem Bearer auth, erros canonicos, `retryable` e `Idempotency-Key`.
- AC3: PASS — smokes cobrem API, CLI, MCP local e MCP remoto por suites executaveis.
- AC4: PASS — docs operacionais nao publicam token em URL/schema/log; testes cobrem rota legada e redaction.
- AC5: PASS — gates e smoke integrado registrados nesta story.

### Evidence

- `npm run lint`: PASS, 7 warnings preexistentes.
- `npm run typecheck`: PASS.
- `npm test`: PASS, 36 testes.
- `node --test packages/shared/test/adapter-parity.test.mjs`: PASS, 3/3.
- `npm run test:integration --workspace @paridade-risco/api`: PASS, 11/11.
- `npm run build:api`: PASS por exit code 0, com warning conhecido de SWC/lockfile no Next.js.

### Files Modified During Review

- Nenhum arquivo adicional durante QA; a story foi atualizada com o resultado.

### Gate Status

Gate: PASS

### Recommended Status

✓ Done — documentacao e smoke tests multi-interface concluidos.

## Change Log

- 2026-07-10: Story criada a partir da auditoria multi-interface.
- 2026-07-10: PO gate aprovado; template, rastreabilidade e contexto refinados.
- 2026-07-10: Documentacao operacional e smokes multi-interface implementados; story movida para Done.
