/**
 * Re-export shared theme colors.
 *
 * Kept as a thin re-export so existing imports
 * (import { colors } from "../theme/colors") continue to work.
 */
export { colors } from "@paridade-risco/shared";
