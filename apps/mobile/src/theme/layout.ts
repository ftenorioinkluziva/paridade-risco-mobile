/**
 * Re-export shared theme layout.
 *
 * Kept as a thin re-export so existing imports
 * (import { layout } from "../theme/layout") continue to work.
 */
export { layout } from "@paridade-risco/shared";
