name: Deploy API

on:
  push:
    branches: [master]
    paths:
      - "apps/api/**"
      - "apps/remote-mcp/**"
      - "packages/shared/**"
      - "packages/cli/**"
      - "packages/local-mcp/**"
      - ".github/workflows/deploy-api.yml"
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}
  cancel-in-progress: true

env:
  REMOTE_DIR: /root/paridade-risco-v2
  CODE_DIR: /root/paridade-risco-v2/code

jobs:
  deploy:
    name: Build & Deploy
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install SSH key
        run: |
          mkdir -p ~/.ssh
          echo "${{ secrets.SSH_KEY }}" > ~/.ssh/id_ed25519
          chmod 600 ~/.ssh/id_ed25519
          ssh-keyscan -H ${{ secrets.SSH_HOST }} >> ~/.ssh/known_hosts

      - name: Sync code to server
        run: |
          rsync -az --no-t --delete \
            --include='*/' \
            --include='package.json' \
            --include='package-lock.json' \
            --include='apps/api/package.json' \
            --include='apps/api/Dockerfile' \
            --include='apps/api/docker-compose.hetzner.yml' \
            --include='apps/api/.env.production' \
            --include='apps/api/drizzle.config.ts' \
            --include='apps/api/next.config.ts' \
            --include='apps/api/tsconfig.json' \
            --include='apps/api/src/**' \
            --include='apps/api/drizzle/**' \
            --include='apps/api/public/**' \
            --include='apps/remote-mcp/**' \
            --include='packages/shared/**' \
            --include='packages/cli/**' \
            --include='packages/local-mcp/**' \
            --exclude='*' \
            ./ root@${{ secrets.SSH_HOST }}:$CODE_DIR/

      - name: Install deps & build
        run: |
          ssh root@${{ secrets.SSH_HOST }} "cd $CODE_DIR && rm -rf node_modules && DATABASE_URL='${{ secrets.DATABASE_URL }}' npm ci && DATABASE_URL='${{ secrets.DATABASE_URL }}' npm run build --workspace=@paridade-risco/api"

      - name: Fix & Build Docker image
        run: |
          ssh root@${{ secrets.SSH_HOST }} "
            cd $CODE_DIR
            sed -i 's# 2>/dev/null || true##g' apps/api/Dockerfile
            docker system prune -af 2>/dev/null || true
            docker build -f apps/api/Dockerfile -t paridade-risco-api:latest --build-arg DATABASE_URL='${{ secrets.DATABASE_URL }}' .
          "

      - name: Deploy container
        run: |
          ssh root@${{ secrets.SSH_HOST }} "
            cd $REMOTE_DIR
            cp code/apps/api/docker-compose.hetzner.yml docker-compose.yml
            cp code/apps/api/.env.production .env 2>/dev/null || true
            docker compose down 2>/dev/null || true
            docker compose up -d
          "

      - name: Run migrations
        run: |
          ssh root@${{ secrets.SSH_HOST }} "
            cd $REMOTE_DIR
            sleep 5
            docker compose exec -T -w /app/apps/api app /app/node_modules/.bin/drizzle-kit migrate
          "

      - name: Seed test user
        run: |
          ssh root@${{ secrets.SSH_HOST }} "
            cd $REMOTE_DIR
            docker compose exec -T -w /app/apps/api app /app/node_modules/.bin/tsx src/db/seed.ts
          "

      - name: Query legacy tables
        run: |
          ssh root@${{ secrets.SSH_HOST }} "
            set -e
            docker compose -f $REMOTE_DIR/docker-compose.yml cp $CODE_DIR/apps/api/src/scripts/query-legacy.cjs app:/tmp/query.cjs
            docker compose -f $REMOTE_DIR/docker-compose.yml exec -T -e NODE_PATH=/app/node_modules app node /tmp/query.cjs
            docker compose -f $REMOTE_DIR/docker-compose.yml exec -T app rm /tmp/query.cjs
          "

      - name: Health check
        run: |
          sleep 5
          curl -sf https://paridaderisco.blackboxinovacao.com.br/api/health && echo "Health OK" || echo "Health endpoint not found"

      - name: Build & Deploy remote-mcp
        run: |
          ssh root@${{ secrets.SSH_HOST }} "
            cd $CODE_DIR
            docker compose --project-name remote-mcp -f apps/remote-mcp/docker-compose.yml down --remove-orphans 2>/dev/null || true
            echo 'Using port 3050 for remote-mcp'
            (ufw status && ufw allow 3050/tcp) || echo 'ufw not available'
            docker system prune -af 2>/dev/null || true
            MCP_PORT=3050 docker compose --project-name remote-mcp -f apps/remote-mcp/docker-compose.yml --env-file $REMOTE_DIR/.env up -d --build
            echo '=== Testing remote-mcp locally ==='
            sleep 3
            curl -sf --connect-timeout 5 http://127.0.0.1:3050/ && echo 'Local OK' || echo 'Local FAIL'
          "

      - name: Configure NPM proxy for remote-mcp
        run: |
          ssh root@${{ secrets.SSH_HOST }} "REMOTE_DIR=$REMOTE_DIR CODE_DIR=$CODE_DIR bash apps/remote-mcp/scripts/configure-npm.sh"
